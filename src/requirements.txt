# Docling RAG Pipeline — Dependencies (Pinned Versions)
# Install into a virtual environment:
#   python -m venv venv
#   venv\Scripts\activate        (Windows)
#   source venv/bin/activate     (Linux/Mac)
#   pip install -r requirements.txt

--extra-index-url https://download.pytorch.org/whl/cu118

# ── Core Document Processing ──
docling==2.98.0

# ── Web UI ──
streamlit==1.58.0

# ── HuggingFace ──
huggingface-hub==1.18.0
python-dotenv==1.2.2
transformers==5.10.2
accelerate==1.13.0
qwen-vl-utils==0.0.14


# ── VLM Figure Description (Qwen3-VL / Qwen2-VL) ──

torch==2.7.1+cu118
torchvision==0.22.1+cu118


# ── Image Processing ──
Pillow==12.2.0

# ── Formatting ──
tabulate==0.9.0

